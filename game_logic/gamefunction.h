#pragma once
int changeRandomlyCharPosition(size_t randPosition,
                               size_t pos,
                               const char* word);

void mixCharInWord(const char* word);

void changeCharAtPosition(char* word,
                          size_t posFirst,
                          size_t posSecond);
